# 公众号数据工具 - 打包说明

## 什么是打包？

打包就是将Python脚本转换成**独立的可执行文件（.exe）**，这样其他电脑上即使没有安装Python也能直接运行！

---

## 📋 快速开始（开发者）

### 第一步：构建可执行文件

1. 确保你的电脑已安装 **Python 3.8 或更高版本**
2. 双击 `构建程序.bat` 文件
3. 等待构建完成（首次可能需要3-5分钟）

构建成功后，会在 `dist` 文件夹中生成 `公众号数据工具.exe`

---

## 🎁 如何分发程序

### 给其他用户使用

构建完成后，只需要将以下内容复制给用户：

```
dist/
└── 公众号数据工具.exe  ← 这就是可以直接运行的程序！
```

用户拿到后：
1. 将 `公众号数据工具.exe` 放到任意文件夹
2. **双击运行**即可！
3. 程序会自动在同目录下创建 `data` 和 `output` 文件夹

---

## 🔧 详细步骤

### 1. 环境准备（仅开发者需要）

确保已安装Python 3.8+，然后在命令行运行：

```bash
# 安装PyInstaller（打包工具）
pip install pyinstaller

# 安装项目依赖
pip install -r requirements.txt
```

### 2. 执行打包

**方式一（推荐）：双击构建脚本**
- 直接双击 `构建程序.bat`

**方式二：命令行手动执行**
```bash
pyinstaller --clean 公众号数据工具.spec
```

### 3. 测试可执行文件

打包完成后：
1. 进入 `dist` 文件夹
2. 双击 `公众号数据工具.exe`
3. 确认程序能正常启动和运行

---

## 📁 文件说明

| 文件 | 说明 |
|------|------|
| `main.py` | 程序入口文件 |
| `requirements.txt` | Python依赖库列表 |
| `公众号数据工具.spec` | PyInstaller配置文件 |
| `构建程序.bat` | 一键构建脚本（Windows） |
| `dist/公众号数据工具.exe` | 打包生成的可执行文件 |
| `build/` | 构建临时文件（可删除） |

---

## ⚠️ 常见问题

### Q: 打包后运行报错 "No module named ..."
A: 在 `公众号数据工具.spec` 文件的 `hiddenimports` 中添加缺失的模块名。

### Q: 可执行文件太大了怎么办？
A: 这是正常的，因为包含了Python解释器和所有依赖库。通常大小在50-150MB之间。

### Q: 杀毒软件报警怎么办？
A: 这是误报，因为PyInstaller打包的程序常被杀毒软件关注。添加信任即可。

### Q: 能在Mac或Linux上运行吗？
A: 当前配置是针对Windows的。如需跨平台，需要在对应系统上重新打包。

---

## 📞 需要帮助？

如果打包过程中遇到问题，请检查：
1. Python版本是否 >= 3.8
2. 所有依赖是否正确安装
3. 是否有足够的磁盘空间（建议至少500MB可用）

---

**祝您打包顺利！** 🎉
